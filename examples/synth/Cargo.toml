[package]
name = "synth"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
rui = { workspace = true }
rodio = { version = "0.20.1", default-features = false, features = [
    "symphonia-all",
] }
